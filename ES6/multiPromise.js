//Date: 10/22/2024

//1.Promise.all([a,b,c])


//2.Promise.allSettled([a,b,c])
//Upon completion of each promisses that we start, it returns the data for each promise individually
//Not all the promisses get rejected if one is reject we get data for each promise object
